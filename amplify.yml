version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd app
        - npm ci
    build:
      commands: -env-cmd -f .env.${BUILD_ENV} npm run build
  artifacts:
    baseDirectory: ./app/.next
    files:
      - "**/*"
  cache:
    paths:
      - ./app/node_modules/**/*
